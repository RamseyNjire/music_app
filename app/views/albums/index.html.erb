<h1> <a href="<%= band_url(@band) %>" class="band_name"><%= @band.name %>'s</a> Albums</h1>

<% @albums.each do |album| %>
    <div class="band_card">
        <%= render album %>
        <a href="<%= album_url(album) %>" class="login_signup_button">View Album</a>
    </div>
<% end %>